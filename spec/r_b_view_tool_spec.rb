require "spec_helper"

describe RBViewTool do
  it "has a version number" do
    expect(RBViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end